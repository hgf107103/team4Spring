<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.team4.travel.object.categoryMapper">
    
    <select id="selectNow" resultType="com.team4.travel.object.categoryVO">
        SELECT * FROM categoryTable LIMIT 1;
    </select>
    
    <!-- <select id="아이디" resultType="결과타입 (맵이없을경우)" resultMap="맵이 있을경우">
    	쿼리
    </select> -->
</mapper>
